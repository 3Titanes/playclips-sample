const DEFAULT_BASE_URL = 'https://s3-eu-west-1.amazonaws.com/media-stg.svplayclips.com/sandbox/'
const DEFAULT_QUALITY = 'medium';

const { createElement, render, Component } = preact;
const h = createElement;